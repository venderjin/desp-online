import React, { useEffect } from "react";
import Modal from "react-modal";

const ChracterPlayVideoModal = ({ onClose }) => {
    return (
        <div style={{ backgroundColor: "white", padding: "20px" }}>
            <p>Bye</p>
            <button>Close</button>
        </div>
    );
};

export default ChracterPlayVideoModal;
