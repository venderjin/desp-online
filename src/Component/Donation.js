import React from "react";

const Donation = () => {
    return (
        <div className="contents">
            <h1>Donation</h1>
            <button>Donation 바로가기</button>
        </div>
    );
};

export default Donation;
