import React from "react";

const Home = () => {
    return (
        <div className="App">
            <h1>Home</h1>
            <button>Home 바로가기</button>
        </div>
    );
};

export default Home;
