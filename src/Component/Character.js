import React from "react";

const Character = () => {
    return (
        <div className="App">
            <h1>Character</h1>
            <button>Character 바로가기</button>
        </div>
    );
};

export default Character;
