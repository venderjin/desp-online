import React, { useState } from "react";

const Ranking = () => {
    return (
        <div className="contents">
            <h1>Ranking</h1>
            <button>X</button>
        </div>
    );
};

export default Ranking;
