import React from "react";

const Ranking = () => {
    return (
        <div className="App">
            <h1>Ranking</h1>
            <button>Ranking 바로가기</button>
        </div>
    );
};

export default Ranking;
